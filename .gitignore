# ── secrets — NEVER commit (real values live in Streamlit Cloud → Secrets) ──
.streamlit/secrets.toml
secrets.toml
.env
*.env

# ── local SQLite database (HR data; production uses Supabase Postgres) ──
data/
*.db
*.sqlite
*.sqlite3

# ── python ──
__pycache__/
*.py[cod]
*.bak

# ── OS / editor noise ──
.DS_Store
Thumbs.db
.vscode/
.idea/

# NOTE: .streamlit/secrets.toml.example IS committed (it is a template with no
# real values). .streamlit/config.toml IS committed (theme, no secrets).
